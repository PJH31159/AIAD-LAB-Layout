# AIAD 연구실 공간 배치 편집기 v4 설계

## 1. 프로젝트 개요

AIAD 연구실 공간 배치 편집기는 줄자로 실측한 평면 치수를 `mm` 단위로 입력하고, 가구와 공간 요소가 실제 공간에 들어가는지 2D 편집·3D 검토·분석 보고서에서 확인하는 데스크톱 웹 도구입니다. 주요 사용자는 연구실 구성원과 공간 배치 담당자이며, 1024px 이상의 PC 브라우저와 GitHub Pages 정적 배포 환경을 기준으로 합니다.

해결하려는 문제는 다음과 같습니다.

- 실측 외곽 공간과 가구 치수를 하나의 좌표계에서 관리합니다.
- 여러 가구를 이동·회전·크기 조절하며 겹침과 외곽 이탈을 즉시 확인합니다.
- 통로 확보 여부를 검토할 수 있도록 최소 통로 폭 설정을 데이터에 보존합니다.
- 여러 배치안을 브라우저에 보관·전환하고 JSON 및 PNG로 공유합니다.

3D는 배치 검토용으로 지원합니다. 실측 높이와 카탈로그 기본 높이를 데이터에서 구분하며, 기본 높이를 실측값처럼 표시하지 않습니다. 분석은 회전 기하와 공간 소속을 사용하고, 이동 경로를 완전히 판정하지 못하는 값은 UI와 PDF에서 근사값으로 명시합니다.

## 2. 현재 프로젝트 분석

### 현재 기술과 구조

- React 19, TypeScript, Vite, Zustand, 선언형 SVG로 구성된 단일 페이지 편집기입니다.
- `App`은 프로젝트·보기·오버레이 흐름을 조정하고, 편집 상태와 히스토리는 Zustand store가 관리합니다.
- 좌표·벽 기하·충돌·분석·직렬화·PNG/PDF 출력은 `src/utils`의 순수 유틸리티로 분리되어 있습니다.
- 브라우저 `localStorage` 자동 저장과 JSON 호환 계층을 기본으로 사용하고, 환경 변수가 있는 경우에만 Supabase 공용 배치안을 활성화합니다.
- Vite의 상대 경로 빌드와 GitHub Pages Actions를 사용합니다.

### 구현된 기능

- 불규칙 다각형 연구실 외곽선, 유리 파티션, 문, 구조물, 가구를 Canvas에 표시합니다.
- 꼭짓점·벽 편집, 이동, 속성 변경, 잠금, 회전, 복제, 삭제를 지원합니다.
- 확대·축소·패닝·격자 스냅, 50단계 실행 취소/다시 실행을 지원합니다.
- 3개 예제 배치안, 점유율·좌석·목표 수량 분석, 여러 배치 경고를 제공합니다.
- 자동·수동 저장, v1 데이터 마이그레이션, JSON 입출력, PNG 및 인쇄 출력을 제공합니다.

### 이번 UI 개선 전 확인한 문제

- 객체 수가 늘었지만 라이브러리에 검색과 빈 결과 복구 동작이 없었습니다.
- 즉시 반영되는 캔버스 설정과 여러 항목을 고르는 내보내기 옵션이 모두 같은 체크박스로 표현되어 선택 방식의 차이가 불명확했습니다.
- 확대·축소가 버튼에만 의존했고, 비동기 작업은 텍스트만 바뀌어 진행 상태를 빠르게 알아보기 어려웠습니다.
- 일부 메뉴는 바깥 클릭과 Escape 닫기, 오류 토스트는 긴 노출과 강한 라이브 알림이 일관되지 않았습니다.
- 설계 문서에 마이그레이션 전 정적 앱 설명과 사용하지 않는 색상 토큰이 남아 있었습니다.

### 보존한 요소

- 불규칙 연구실 외곽, `mm` 영속 좌표, 벽별 두께, 문·창문 부착과 공유 기하 계약을 유지합니다.
- 격자·스냅, 이동·회전·복제·삭제·잠금, 히스토리, 자동 저장과 JSON/PNG/PDF 출력을 유지합니다.
- v2·v3·v4 JSON 호환 계층과 기존 로컬 저장 키를 유지합니다.
- 겹침, 기둥 충돌, 외곽 이탈, 통로 경고와 객체 종류별 도면 기호·색을 유지합니다.

### 독립 구현 원칙

- 공개 디자인 문서는 정보 위계, 상태 피드백, 접근성, 컴포넌트 책임을 판단하는 참고 자료로만 사용합니다.
- 외부 디자인 시스템 패키지, Provider, API·SDK, 서버, 로고·에셋, 소스 코드, CSS와 토큰은 설치하거나 복제하지 않습니다.
- 프로젝트의 React·TypeScript·Zustand·SVG 구조와 색상 체계를 유지하며 필요한 UI는 기존 컴포넌트와 CSS로 직접 구현합니다.
- 모바일 전용 하단 시트·고정 CTA·키패드·동의 흐름은 데스크톱 CAD 작업과 맞지 않으므로 추가하지 않습니다.

## 3. 기술 스택 결정

### 선택

- **React 19 + TypeScript**: 선택 상태를 객체 라이브러리, SVG 캔버스, 속성 패널, 상태 바가 공유하므로 선언적 컴포넌트 구조와 정적 타입이 적합합니다.
- **Vite**: 빠른 개발 서버와 GitHub Pages용 정적 번들을 제공하며 서버 런타임이 필요 없습니다.
- **SVG**: 가구 수가 수백 개 미만인 현재 용도에서 선택·키보드 접근성·핸들·텍스트·치수선 구현이 Canvas보다 명확합니다. 좌표는 `viewBox`가 아니라 명시적 mm→px 변환 유틸리티를 통해 표시합니다.
- **Zustand**: 편집 상태, 선택, 히스토리, 뷰 설정을 작은 액션 단위로 관리합니다. Context와 reducer 조합보다 보일러플레이트가 적고 UI 외부 저장 구독도 단순합니다.
- **CSS 단일 토큰/컴포넌트 클래스 구조**: 현재 규모에서는 CSS Modules 다수 파일보다 전역 토큰과 명확한 BEM형 클래스가 일관된 데스크톱 레이아웃 관리에 적합합니다. 프리텐다드 400·500·600·700 WOFF2를 정적 번들에 포함해 운영체제 글꼴 설치 여부와 무관하게 동일한 한글 조판을 제공합니다.
- **localStorage**: 현재 작업과 최대 30개의 보관 배치안 데이터는 작고 오프라인·GitHub Pages에서 즉시 동작합니다. 데이터가 커지거나 썸네일·검색이 필요해지면 IndexedDB 저장 어댑터로 교체합니다.

### 고려했으나 선택하지 않은 대안

- **react-konva**: 대량 객체 렌더링에는 유리하지만 이번 범위에서는 별도 Canvas 장면 그래프와 Transformer API가 추가 복잡성을 만듭니다.
- **Fabric.js**: 범용 그래픽 편집 기능이 많지만 mm 도메인 모델과 저장 형식을 라이브러리 객체에 종속시킬 가능성이 큽니다.
- **Redux Toolkit**: 현재 상태 규모에 비해 액션·리듀서 구성이 무겁습니다.
- **IndexedDB**: 현재 단일 프로젝트 데이터에는 localStorage로 충분합니다.

## 4. 정보 구조

```text
┌─────────────────────────────────────────────────────────────────┐
│ [아이콘] 배치안명  실행 취소  다시 실행  저장  JSON  PNG  패널 │
├──────────────┬──────────────────────────────────┬───────────────┤
│ 공간 요소     │                                  │ 선택 객체 속성 │
│ 벽 / 문       │                                  │ 이름 / 종류    │
│ 창문 / 기둥   │          2D SVG 캔버스            │ 크기 / 위치     │
│ 콘센트 / LAN  │                                  │ 회전 / 잠금     │
│              │                                  │ 복제 / 삭제     │
│ 가구          │                                  │ 경고            │
│ 책상 / 의자   │                                  │               │
│ 수납 / 선반   │                                  │               │
├──────────────┴──────────────────────────────────┴───────────────┤
│ − 100% +  화면 맞춤     격자 50 mm  스냅 켜짐  단위 mm          │
└─────────────────────────────────────────────────────────────────┘
```

중앙 캔버스가 항상 남는 3열 구조를 사용합니다. 1440px 이하에서는 패널 폭을 줄이고, 1100px 이하에서는 일부 저빈도 툴바 명령을 아이콘으로 축약합니다. 각 패널은 독립적으로 접을 수 있으며 1024px에서도 가로 잘림 없이 중앙 캔버스를 유지합니다. 모바일 전용 재배치는 제공하지 않습니다.

## 5. 사용자 흐름

```text
프로젝트 열기
→ 연구실 외곽 확인 또는 크기 설정
→ 문·창문·기둥 등 공간 요소 배치
→ 가구 추가
→ 실제 너비와 깊이 입력
→ 위치·회전·크기 조정
→ 충돌 및 외곽 이탈 확인
→ 배치안 자동/수동 저장
→ JSON 백업 또는 PNG 내보내기
```

라이브러리 항목은 클릭하면 화면 중앙에 추가되고, 드래그하면 놓은 위치에 추가됩니다. 객체 선택 후 캔버스 직접 조작과 우측 수치 입력을 병행합니다.

## 6. 디자인 원칙

- CAD 도구의 명확한 책임 분리와 캔버스 중심 조작에 공개 모바일 디자인 문서의 정보 위계·상태 피드백 원칙을 독립적으로 적용합니다.
- 중앙 캔버스를 가장 넓게 유지하고 좌측은 생성, 우측은 편집 책임으로 분리합니다.
- 상단에는 프로젝트·파일·히스토리 작업만, 하단에는 뷰 상태만 둡니다.
- 기능을 숨기지 않되 낮은 빈도의 설정은 객체 미선택 상태의 우측 패널에 둡니다.
- 글래스모피즘, 그라데이션, 이모지, 장식성 그림자를 사용하지 않습니다.
- 제목 위의 영문 보조 라벨처럼 의미가 중복되는 장식 텍스트는 사용하지 않습니다.
- 모서리는 입력과 버튼에만 6~8px 범위로 제한하고 패널 자체는 둥글게 만들지 않습니다.
- 충돌은 색상뿐 아니라 경고 아이콘과 문장으로 함께 표시합니다.

### 구조 와이어프레임 검토와 최종 UI 규칙

- 저충실도 단계에서 상단 프로젝트 작업, 좌측 생성, 중앙 도면 조작, 우측 속성 편집, 하단 보기 상태의 책임을 먼저 고정합니다.
- 최종 UI는 객체 검색과 종류 탭을 좌측 상단에 두고, 선택 상태와 편집 필드는 우측의 연속된 섹션으로 유지합니다.
- 주요 동작은 채움 버튼, 보조 동작은 외곽선 또는 텍스트 버튼, 위험 동작은 위험색과 확인 문장을 함께 사용합니다.
- 분석 화면은 통계 카드 묶음 대신 제목·설명·좌우 값으로 구성한 정보 행을 사용합니다. 보고서 저장은 분석의 주 작업이 아니므로 별도 강조 CTA가 아니라 설명 옆의 약한 보조 버튼으로 표시합니다.
- 이진 설정은 `role="switch"`, 다중 선택은 체크박스, 단일 보기 전환은 탭·세그먼트로 구분합니다.
- 아이콘 버튼은 접근성 이름과 포커스 가능한 도움말을 제공하고, 메뉴는 바깥 클릭과 Escape로 닫습니다.
- 가시 UI 글자는 보조 키캡과 도면 내부 치수를 제외하고 11px 이상을 사용하며, 포인터 조작 대상은 최소 32px의 유효 영역을 제공합니다.
- 작은 콘센트·포트·벽 끝점·꼭짓점처럼 도면 축척상 작게 보이는 요소는 시각 크기와 별개로 36px 투명 히트 영역을 제공합니다.
- 3D 제어는 2D 툴바와 같은 36px 조작 높이와 세그먼트·필드·보조 버튼 위계를 사용하며 카메라 항목을 줄바꿈하지 않습니다.
- 1240px 이하에서는 상단과 중복되는 하단 중앙 요약을 숨기고 줌·스냅·저장 상태를 우선합니다.
- 비동기 작업은 로더·스켈레톤과 상태 문장을 함께 사용하며, 오류는 `role="alert"`, 일반 완료는 `role="status"`로 알립니다.
- 모달은 네이티브 `dialog`의 포커스 범위, Escape 닫기, 트리거 포커스 복원을 사용합니다. 정보·탐색형 모달은 헤더의 X 하나로 닫고, 실행·확인형 모달은 헤더 X 없이 푸터의 취소·실행 버튼만 제공하여 동일한 닫기 동작을 중복하지 않습니다.

### 디자인 토큰

```css
--app-background: #f6f7f9;
--panel-background: #ffffff;
--canvas-background: #ffffff;
--border-color: #e8ebef;
--grid-color: #e8ebef;
--text-primary: #343740;
--text-secondary: #6b7684;
--text-disabled: color-mix(in srgb, #6b7684 48%, #ffffff);
--accent: #4e5968;
--accent-strong: #4e5968;
--accent-hover: #343740;
--accent-light: #f6f7f9;
--danger: #f15b66;
--danger-light: color-mix(in srgb, #f15b66 10%, #ffffff);
--warning: #daa952;
```

### TDS Mobile 공개 문서 전수 검토표

아래 항목은 2026-08-16 공개 사이드바 문서를 모두 확인한 결과입니다. `적용`과 `변형 적용`은 TDS 구현체를 가져온다는 뜻이 아니라, 현재 편집기의 기존 React 컴포넌트와 CSS에 해당 사용성 원칙을 독립 구현했다는 뜻입니다.

| 문서 | 결정 | 현재 프로젝트 연결 및 구현 결과 |
|---|---|---|
| 소개 | 참고만 함 | 적용 범위와 디자인 철학을 확인했으며 구현 대상은 아닙니다. |
| 시작하기 | 참고만 함 | 패키지 설치 절차는 사용하지 않고 컴포넌트 책임만 참고했습니다. |
| Colors | 변형 적용 | 기존 팔레트와 기능 색상을 유지하면서 의미별 토큰을 정리했습니다. |
| Typography | 변형 적용 | 데스크톱 도구에 맞춰 제목·본문·라벨·보조문 위계를 11–18px로 재정리했습니다. |
| Badge | 변형 적용 | 선택, 경고 수, 저장 상태를 작은 상태 표식으로 구현했습니다. |
| Board Row | 변형 적용 | 도면 요약과 상태 정보를 연속 정보 행으로 구성했습니다. |
| Border | 적용 | 패널, 입력, 행 구분선을 단일 저대비 경계 체계로 통일했습니다. |
| Bottom Info | 변형 적용 | 모달 푸터의 보조 설명과 상태 문장에만 원칙을 적용했습니다. |
| Bottom Sheet | 제외 | 모바일 하단 제스처 표면은 데스크톱 CAD 작업에 맞지 않습니다. |
| Bubble | 제외 | 대화형 말풍선 사용 사례가 없습니다. |
| Button | 적용 | 주요·보조·약한·위험 버튼과 상태를 통일했습니다. |
| Checkbox | 적용 | 내보내기처럼 복수 옵션을 선택하는 경우에만 사용합니다. |
| Grid List | 변형 적용 | 객체 카탈로그를 데스크톱 단일 열 고밀도 목록으로 변형했습니다. |
| Highlight | 변형 적용 | 선택, 활성 도구, 검색 탭의 약한 배경 강조에 적용했습니다. |
| Icon Button | 적용 | 36–40px 클릭 영역, 접근성 이름, 포커스 도움말을 적용했습니다. |
| List Footer | 변형 적용 | 목록의 빈 상태와 보조 안내 영역에 적용했습니다. |
| List Header | 적용 | 라이브러리·속성·분석 섹션 제목과 개수/설명을 통일했습니다. |
| Loader | 적용 | 저장, 공용 배치안, 3D 지연 로딩에 상태 문장과 함께 사용합니다. |
| Menu | 적용 | 파일·도면 메뉴의 행 구조, 바깥 클릭, Escape, 포커스 복원을 적용했습니다. |
| Modal | 적용 | 네이티브 dialog 포커스 범위와 단일 닫기 방식으로 통일했습니다. |
| Numeric Spinner | 변형 적용 | mm 수치 입력과 단위 표시로 변형했으며 강제 증감 버튼은 추가하지 않았습니다. |
| Paragraph | 적용 | 도움말·경고·빈 상태의 제목과 설명 위계를 통일했습니다. |
| Post | 제외 | 피드형 콘텐츠가 없어 추가하지 않았습니다. |
| Progress Bar | 적용 | 분석의 면적 사용 비율을 정량 막대로 표시합니다. |
| Progress Stepper | 제외 | 다단계 완료 흐름이 없어 추가하지 않았습니다. |
| Rating | 제외 | 평가 입력 사용 사례가 없습니다. |
| Result | 변형 적용 | 검색 없음, 경고 없음, 보관 배치안 없음 상태로 변형했습니다. |
| Search Field | 적용 | 객체 이름 검색, 지우기, 빈 결과 복구를 구현했습니다. |
| Segmented Control | 적용 | 2D/3D, 문 방향처럼 상호 배타적인 전환에 사용합니다. |
| Skeleton | 적용 | 원격 공용 배치안 로딩에 사용합니다. |
| Slider | 적용 | 줌 범위 입력과 접근성 값을 제공합니다. |
| Stepper | 변형 적용 | 줌의 감소·값·슬라이더·증가 조작으로 변형했습니다. |
| Switch | 적용 | 즉시 반영되는 캔버스 설정에 role=switch로 사용합니다. |
| Tab | 적용 | 객체 종류 필터와 키보드 좌우 이동을 구현했습니다. |
| Table Row | 적용 | 분석 수치를 라벨·값 정보 행으로 표시합니다. |
| Text Button | 적용 | 낮은 우선순위의 도움말·취소·복구 동작에 사용합니다. |
| Toast | 적용 | 저장·가져오기·내보내기 결과를 status/alert로 알립니다. |
| Tooltip | 적용 | 아이콘 도구에 키보드로도 노출되는 짧은 설명을 제공합니다. |
| Top | 변형 적용 | 앱 상단을 브랜드·현재 배치안·프로젝트 작업 영역으로 구성했습니다. |
| Agreement V3 | 제외 | 약관 동의 흐름이 없습니다. |
| Agreement V4 | 제외 | 약관 동의 흐름이 없습니다. |
| Asset 이해하기 | 참고만 함 | 자산 사용 원칙만 확인했으며 토스 자산은 사용하지 않습니다. |
| Asset 활용하기 | 참고만 함 | 프로젝트 자체 SVG 아이콘과 도면 기호를 유지합니다. |
| 래핑한 컴포넌트 활용하기 | 참고만 함 | TDS 래퍼를 만들지 않고 현재 컴포넌트를 직접 정리했습니다. |
| BottomCTA 이해하기 | 변형 적용 | 실행형 모달의 푸터 작업 위계에만 원칙을 반영했습니다. |
| Single | 변형 적용 | 단일 확인 또는 저장 작업을 모달 푸터 버튼으로 구현했습니다. |
| Double | 변형 적용 | 취소·실행 쌍이 필요한 모달에만 사용합니다. |
| FixedBottomCTA | 제외 | 앱 하단 고정 CTA는 상태바와 충돌하므로 사용하지 않습니다. |
| Bar Chart | 변형 적용 | 분석 비율을 단일 수평 막대로 단순화했습니다. |
| Dialog 이해하기 | 적용 | 정보형과 실행형 다이얼로그의 닫기 책임을 분리했습니다. |
| AlertDialog | 적용 | 경고·오류 확인을 문장과 역할로 제공합니다. |
| ConfirmDialog | 적용 | 초기화·삭제·충돌 해결에 인라인 또는 실행형 확인을 사용합니다. |
| Alphabet Keypad | 제외 | 물리 키보드가 기본인 PC 편집기에 불필요합니다. |
| Full Secure Keypad | 제외 | 보안 번호 입력 기능이 없습니다. |
| Number Keypad | 제외 | 치수 입력은 네이티브 수치 필드와 물리 키보드를 사용합니다. |
| ListRow 이해하기 | 적용 | 아이콘·주 정보·보조 정보·후행 작업의 행 책임을 적용했습니다. |
| ListRow 영역 구성하기 | 적용 | 객체, 메뉴, 저장 배치안, 경고 목록의 영역을 일관되게 구성했습니다. |
| ListRowLegacy | 참고만 함 | 레거시 API 이력 문서이므로 구현하지 않았습니다. |
| v3에서 제거되는 API | 참고만 함 | 마이그레이션 주의점만 확인했으며 의존성이 없습니다. |
| TextField | 적용 | 라벨·값·단위·오류·포커스 상태를 통일했습니다. |
| SplitTextField | 제외 | 분할 코드나 인증번호 입력 사례가 없습니다. |
| TextArea | 적용 | 프로젝트 설명과 긴 텍스트 입력에 사용합니다. |
| Overlay Extension | 참고만 함 | 오버레이 책임 분리 원칙만 확인했습니다. |
| Overlay Extension 이해하기 | 참고만 함 | 외부 확장 API 없이 현재 App 상태로 오버레이를 관리합니다. |
| useDialog | 참고만 함 | 별도 훅을 복제하지 않고 공통 ModalFrame 책임에 반영했습니다. |
| useToast | 참고만 함 | 별도 훅을 복제하지 않고 기존 Toast 흐름을 유지했습니다. |
| useBottomSheet | 제외 | Bottom Sheet를 사용하지 않으므로 필요하지 않습니다. |
| @toss-design-system에서 마이그레이션 | 참고만 함 | 현재 프로젝트는 TDS 의존성이 없어 마이그레이션 대상이 아닙니다. |
| v1에서 v2로 마이그레이션 | 참고만 함 | TDS 버전 마이그레이션 대상이 아니며 자체 JSON v1–v4 호환만 유지합니다. |

## 7. 데이터 모델

모든 영속 좌표와 치수는 `mm`입니다. 줌과 패닝은 저장 프로젝트와 분리된 UI 상태입니다.

```ts
type LayoutObjectType =
  | "wall" | "door" | "window" | "column" | "outlet" | "lan-port"
  | "desk" | "meeting-table" | "chair" | "cabinet" | "shelf"
  | "partition" | "custom";

type LayoutObject = {
  id: string;
  type: LayoutObjectType;
  name: string;
  x: number;
  y: number;
  width: number;
  depth: number;
  rotation: number;
  locked: boolean;
};

type RoomData = {
  name: string;
  vertices: Array<{ x: number; y: number }>;
  wallThicknesses: number[];
};

type LayoutSettings = {
  unit: "mm";
  gridSize: number;
  snapEnabled: boolean;
  objectSnapEnabled: boolean;
  orthogonalSnapEnabled: boolean;
  minimumAisleWidth: number;
  showGrid: boolean;
  showLabels: boolean;
  showDimensions: boolean;
};

type LayoutProject = {
  version: 4;
  projectName: string;
  room: RoomData;
  objects: LayoutObject[];
  settings: LayoutSettings;
  updatedAt: string;
};
```

## 8. 컴포넌트 구조

```text
src/
├─ components/
│  ├─ icons/Icon.tsx
│  └─ layout-editor/
│     ├─ EditorToolbar.tsx
│     ├─ ObjectLibrary.tsx
│     ├─ LayoutCanvas.tsx
│     ├─ PropertiesPanel.tsx
│     ├─ EditorStatusBar.tsx
│     ├─ ModalFrame.tsx
│     ├─ RoomSetupDialog.tsx
│     ├─ WarningsDialog.tsx
│     ├─ AnalysisDialog.tsx
│     ├─ ThreeDView.tsx
│     ├─ ProjectDialog.tsx
│     ├─ ExportDialog.tsx
│     └─ Toast.tsx
├─ data/objectCatalog.ts
├─ store/layoutStore.ts
├─ types/layout.ts
├─ utils/
│  ├─ coordinates.ts
│  ├─ collision.ts
│  ├─ snapping.ts
│  ├─ serialization.ts
│  ├─ exportPng.ts
│  ├─ exportPdf.ts
│  └─ wallGeometry.ts
├─ services/supabaseProjects.ts
├─ App.tsx
├─ main.tsx
└─ styles.css
```

## 9. 구현 단계

1. React·TypeScript·Vite 기반과 3단 편집 레이아웃을 구성합니다.
2. mm 좌표 모델, 카탈로그, SVG 렌더링을 구현합니다.
3. 클릭/드래그 추가, 선택, 이동, 속성 수정을 구현합니다.
4. 회전·크기 핸들, 복제·삭제·잠금, 키보드 단축키를 구현합니다.
5. 확대·축소·패닝·격자·실제 mm 스냅·화면 맞춤을 구현합니다.
6. Zustand 히스토리와 localStorage 자동 저장, JSON 입출력을 구현합니다.
7. 객체 겹침·기둥 충돌·외곽 이탈 경고와 정렬 보조선을 구현합니다.
8. 벽별 두께, 분할·병합, 문·창문 부착을 SVG·PNG·PDF·3D 공통 기하로 구현합니다.
9. 분석·PDF, 3D 검토, 인증 기반 Supabase 공용 배치안을 구현합니다.
10. lint, 테스트, 빌드와 GitHub Pages Actions를 구성하고 검증합니다.

## 10. 완료 조건

- 1024×900, 1280×900, 1440×900, 1920×1080에서 가로 잘림 없이 중앙 작업 영역이 유지되고 패널을 접을 수 있습니다.
- 모든 프로젝트 치수는 mm로 저장되며 줌·창 크기 변경 후에도 변하지 않습니다.
- 필수 공간 요소와 가구를 클릭 또는 드래그로 추가할 수 있습니다.
- 선택, 이동, 크기 변경, 회전, 복제, 삭제, 잠금이 캔버스·속성 패널·단축키에서 일관되게 동작합니다.
- 실행 취소/다시 실행, 자동/수동 저장, 새 프로젝트, JSON 입출력, PNG 출력이 오류 경계를 포함해 동작합니다.
- 가구 겹침, 기둥 충돌, 외곽 이탈이 시각·텍스트로 표시됩니다.
- 전역 경고 목록에서 문제 객체를 선택하고 캔버스 중앙으로 이동할 수 있습니다.
- 네이티브 다이얼로그가 초기 포커스, 포커스 범위 제한, Escape 닫기와 트리거 포커스 복원을 지원합니다.
- 2D와 3D, PNG와 PDF가 저장된 벽 두께를 동일하게 사용합니다.
- project-main v3.1과 React v3/v4 JSON에서 방향, 격자, 공간 ID, 벽 두께를 보존합니다.
- Supabase 설정이 없거나 원격 검증을 수행하지 못한 경우 원격 기능을 완료로 보고하지 않습니다.
- `npm run lint`, `npm run test`, `npm run build`가 성공합니다.
- Vite `base: "./"`와 Pages Actions를 통해 프로젝트 경로 하위에서 정적 자산이 정상 로드됩니다.
